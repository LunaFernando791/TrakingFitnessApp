<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo transparente -->
    <solid android:color="#202328"/>

    <!-- Borde azul claro tipo "#7899BE" como el de la imagen -->
    <stroke
        android:width="1.5dp"
        android:color="#414656" />

    <!-- Bordes redondeados como en la imagen -->
    <corners android:radius="8dp"/>

    <!-- Espaciado interno para darle aire al ícono -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>
</shape>
